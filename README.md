# Data Pipeline Assignment — Muhammad Rabby Ilyas

## Project description

This project prepares the supplied automobile dataset through a reproducible ETL pipeline. The pipeline will inspect, clean, transform, and save the data as a new processed CSV without modifying the raw input.

## Dataset

- Source: https://s.id/dataset-sesi-3
- Pipeline input: `data/raw/automobileEDA_dirty_training.csv`
- Processed output: `data/processed/automobileEDA_processed.csv`

## Project structure

```text
assignment-data-pipeline-muhammad-rabby-ilyas/
├── data/
│   ├── raw/
│   │   └── automobileEDA_dirty_training.csv
│   └── processed/
│       └── automobileEDA_processed.csv  # generated by pipeline.py
├── src/
│   └── pipeline.py
├── documentation/
│   └── data-flow-diagram.png
├── README.md
└── requirements.txt
```

## Initial dataset condition

TODO: Add the dataset size, missing-value columns, duplicate count, incorrect data types, inconsistent categories, and other important findings after inspection.

## Data cleaning

TODO: Document each problem, affected column, cleaning method, reason for the method, and row counts before and after cleaning.

## Data transformation

TODO: Document the numeric column selected for Min-Max scaling and the categorical column selected for encoding, including the reasons for choosing them.

| Column | Before | After | Method |
| --- | --- | --- | --- |
| TODO | TODO | TODO | TODO |

## ETL flow

1. Extract the raw CSV from `data/raw/`.
2. Inspect, clean, and transform the dataset.
3. Load the result into `data/processed/automobileEDA_processed.csv`.

## Installation

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

## Running the pipeline

```powershell
python src/pipeline.py
```

The raw dataset must remain unchanged. The processed dataset must be generated automatically by `pipeline.py`.
